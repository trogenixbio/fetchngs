process {
    withName: 'SRA_IDS_TO_RUNINFO' {
        publishDir = [
            path: { "${params.outdir}/public/run_info/${params.pipeline_version}-${workflow.runName}-${nextflow.build}/metadata/json" },
            enabled: false
        ]
    }
}
