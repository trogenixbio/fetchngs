process {
    withName: SRA_TO_SAMPLESHEET {
        publishDir = [
            path: { "${params.outdir}/public/run_info/${params.pipeline_version}-${workflow.runName}-${nextflow.build}/metadata/samplesheet" },
            enabled: false
        ]
    }
}
