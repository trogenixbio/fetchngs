process {
    withName: 'CHECK_METADATA' {
        publishDir = [
                path: { "${params.outdir}/public/run_info/${params.pipeline_version}-${workflow.runName}-${nextflow.build}/metadata/validation" },
                mode: params.publish_dir_mode,
                pattern: "*.txt"
        ]
    }
}
